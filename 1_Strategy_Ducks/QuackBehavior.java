//This is the interface of the quack behavior. We will create classes that implement this interface
//so they can override the Quack method. 
public interface QuackBehavior {

	public void quack();
}
